extends ../layout/index

block contenido

        if mensajes 
        div(class="max-w-md mx-auto my-10")
            each mensaje in mensajes
                p.text-indigo-600.text-center.p-2.mb-1.font-bold= mensaje.msg